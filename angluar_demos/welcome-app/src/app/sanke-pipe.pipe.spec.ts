import { SankePipePipe } from './sanke-pipe.pipe';

describe('SankePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SankePipePipe();
    expect(pipe).toBeTruthy();
  });
});
